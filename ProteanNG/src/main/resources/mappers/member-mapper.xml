<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">

<!--     <select id="loginUser" parameterType="Member" resultType="Member">
        SELECT *
          FROM MEMBER 
         WHERE ID = #{id} 
           AND PASSWORD = #{password}
    </select> 
 -->    
  
    <select id="login" parameterType="Member" resultType="Member">
        SELECT *
        FROM MEMBER WHERE ID = #{id} AND PASSWORD = #{password}
    </select>

  
<!--     <insert id="register">
        INSERT INTO MEMBER (membercode, id, password, email, enrolldate, quitdate, department, username)
                    VALUES (MEMBERCODE_SEQ.NEXTVAL, #{id}, #{password}, #{email}, TO_DATE(SYSDATE,'YYYY.MM.DD HH24:MI'),TO_DATE(SYSDATE,'YYYY.MM.DD HH24:MI'), #{department}, #{username})                    
    </insert>  -->
<!-- https://java119.tistory.com/69 들어가서 만들어야됨 -->

</mapper>